import { styled } from '@/styles';

export const Container = styled('div', {
	display: 'inline-flex',
	gap: 4,
	position: 'relative',
	cursor: 'pointer',
	textAlign: 'left',

	color: '$purple100',
});
